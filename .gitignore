.env.local
config/*.cfg
node_modules/
dist/
backups/
test-results/
.project-control-update.patch
*.log

.vercel
.env*

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
